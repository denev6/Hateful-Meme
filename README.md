# Hateful Meme Detection

## Dataset

The Hateful Memes Challenge by Meta ([data](./data))

The training set was split into training and test sets with an 8:2 ratio since the original test set labels are unavailable.

## Run

```sh
python main.py --config-name ca
```
